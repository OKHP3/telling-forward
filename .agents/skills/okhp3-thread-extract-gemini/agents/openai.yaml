interface:
  display_name: "Gemini Thread Extraction"
  short_description: "Evacuate pasted Gemini chats into portable Markdown"
  default_prompt: "Extract the supplied Gemini thread into a standalone Markdown artifact at balanced depth."
policy:
  allow_implicit_invocation: true
