# Supplied capture

- Source platform: unknown
- Capture mode: turn-by-turn
- Completeness: partial

User

The goal is to make eight standalone thread-extraction skills. The generic one
must work across platforms, while each variant recognizes its own UI patterns.

Assistant

Current state: the folders and first drafts exist. Shared files include the
template, depth profiles, extraction contract, and writer utility.

User

Balanced should be the default. Scan means rapid. Catalog means comprehensive.
Do not use the earlier emergency metaphor in the finished skills.

Assistant

Decision recorded. Dates and times should be captured only when supplied and
must never block extraction.

User

Blockers: Microsoft Copilot role labels vary by surface. Images and canvases may
survive only as chips or titles. Clipboard formatting can flatten speaker names.

Assistant

Use explicit labels first, then export fields, response controls, composer and
action rows, and only then low-confidence alternation. Unknown is safer than a
forced role.

User

Critical assets are `platform-capture-patterns.md`, `thread-extract-template.md`,
and `create_thread_extract.py`. The next action is validation against three
realistic evals, followed by catalog regeneration.

Assistant

Routine note repeated for visibility: keep the artifact source-independent and
record every omission. This reminder appeared in several earlier turns.

User

Stop after producing a compact continuity brief. Another agent must be able to
resume without opening this source thread.
