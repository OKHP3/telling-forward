# Supplied capture

- Source platform: mixed or unknown
- Capture mode: full-paste
- Completeness: partial

You

We need a repeatable way to move valuable AI conversations into our repository.
The extract must preserve decisions and reusable prompts, but it must not require
the old chat account. Balanced depth is probably right.

Assistant

Use a turn ledger, an element ledger, a three-pass title chain, and a final
rehydration check. Keep provenance separate from verified truth.

[Image: extraction-flow.png]
Alt text: Raw thread flows through normalization and value extraction into a
Markdown artifact.

Copy
Good response
Sources

You

The PDF named `retention-rules.pdf` contains the private-data rules. I cannot
paste it here. The final artifact should record that gap and default to
`needs-review`.

Assistant

Decision: use balanced depth by default. Preserve the user goal, rationale,
current state, and next actions. Catalog the PDF as referenced but not supplied.
The Canvas called `Depth Mapping` contains a comparison table, but only its title
survived this copy.

Canvas: Depth Mapping
Retry
