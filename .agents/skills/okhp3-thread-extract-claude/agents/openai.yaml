interface:
  display_name: "Claude Thread Extraction"
  short_description: "Evacuate pasted Claude chats into portable Markdown"
  default_prompt: "Extract the supplied Claude thread into a standalone Markdown artifact at balanced depth."
policy:
  allow_implicit_invocation: true
