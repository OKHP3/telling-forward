# Ingestion pipeline dependencies (Tier 0 + Tier 1 from
# docs/adr/0004-manuscript-ingestion-and-bring-your-own-ai.md).
#
# python-docx and pdfplumber cover Tier 0 (rules-only conversion).
# huggingface_hub and llama-cpp-python cover Tier 1 (Actions + Phi-4-mini).
# pandoc itself is a system package, not pip-installable — the workflow
# installs it via apt before this file is used.

python-docx==1.2.0
pdfplumber==0.11.9
huggingface_hub==0.27.1
llama-cpp-python==0.3.35
PyGithub==2.5.0
pytest==8.3.5
