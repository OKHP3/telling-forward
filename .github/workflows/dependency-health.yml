name: Dependency health

on:
  push:
    branches:
      - main
    paths:
      - '**/package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'docs/decisions/provenance-fidelity-contract.md'
      - 'docs/decisions/provenance-fidelity-cases.yaml'
      - 'artifacts/api-server/src/lib/contributor-fidelity.ts'
      - 'artifacts/api-server/src/lib/__tests__/contributor-fidelity.test.ts'
      - 'lib/api-spec/openapi.yaml'
      - 'lib/api-zod/src/generated/**'
      - 'package.json'
      - 'scripts/check-technology-updates.mjs'
      - 'scripts/check-technology-updates.test.mjs'
      - '.github/workflows/dependency-health.yml'
  pull_request:
    paths:
      - '**/package.json'
      - 'pnpm-lock.yaml'
      - 'pnpm-workspace.yaml'
      - 'docs/decisions/provenance-fidelity-contract.md'
      - 'docs/decisions/provenance-fidelity-cases.yaml'
      - 'artifacts/api-server/src/lib/contributor-fidelity.ts'
      - 'artifacts/api-server/src/lib/__tests__/contributor-fidelity.test.ts'
      - 'lib/api-spec/openapi.yaml'
      - 'lib/api-zod/src/generated/**'
      - 'package.json'
      - 'scripts/check-technology-updates.mjs'
      - 'scripts/check-technology-updates.test.mjs'
      - '.github/workflows/dependency-health.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
        with:
          version: 11.19.0
      - uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: node --test scripts/check-technology-updates.test.mjs
      # Registry availability belongs to the scheduled watch, not the PR gate.
      - run: node scripts/check-technology-updates.mjs --offline > technology-inventory-ci.md
      - name: Verify contributor fidelity contract
        run: pnpm run check:contributor-fidelity
      - run: pnpm run typecheck
      - run: pnpm run build
