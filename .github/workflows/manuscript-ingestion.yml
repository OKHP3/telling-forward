# Tier-1 manuscript ingestion, per
# docs/adr/0004-manuscript-ingestion-and-bring-your-own-ai.md.
#
# Converts an uploaded manuscript to markdown, segments it into chapters
# and scenes, runs Phi-4-mini-instruct (CPU inference, local to this
# runner) to draft candidate capsules, and files each as a GitHub Issue
# labeled "capsule" + "state:draft" for human review on the Concept Board.
#
# Cost: GitHub Actions minutes only. Free on public repos; counts against
# the free monthly allotment on private repos. No AI API key is read, no
# AI provider is called, nothing here bills the platform owner.
#
name: Manuscript ingestion (Tier 1)

on:
  workflow_dispatch:
    inputs:
      manuscript_path:
        description: "Path to the uploaded manuscript file within this repo (e.g. intake/my-chapter.docx)"
        required: true
        type: string
      owner:
        description: "Storyworld repo owner"
        required: true
        type: string
      repo:
        description: "Storyworld repo name"
        required: true
        type: string
      ref:
        description: "Branch containing the private intake file"
        required: true
        type: string
      upload_id:
        description: "Stable upload identifier used for retry/concurrency"
        required: true
        type: string

concurrency:
  group: manuscript-ingestion-${{ inputs.owner }}-${{ inputs.repo }}-${{ inputs.upload_id }}
  cancel-in-progress: false

permissions:
  contents: read
  issues: write

env:
  # Pinned to a specific Hugging Face revision (commit hash), not "main" or
  # "latest" — reproducibility without committing the weight file itself.
  HF_MODEL_REPO: "unsloth/Phi-4-mini-instruct-GGUF"
  HF_MODEL_REVISION: "78eb92a46fc37e6b524df991ed9aca9bc6aa7b80"
  HF_MODEL_FILE: "Phi-4-mini-instruct-Q4_K_M.gguf"
  # Published metadata for HF_MODEL_FILE at HF_MODEL_REVISION. Keep these
  # values together with the revision: a cache hit is not trusted by itself.
  HF_MODEL_SIZE_BYTES: "2491874272"
  HF_MODEL_SHA256: "88c00229914083cd112853aab84ed51b87bdf6b9ce42f532d8c85c7c63b1730a"

jobs:
  ingest:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}

      - name: Install pandoc
        run: sudo apt-get update && sudo apt-get install -y pandoc

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # llama-cpp-python is published as an sdist for this runner, so the
      # first install compiles it. Cache pip's downloaded files and built
      # wheel so repeat runs do not rebuild the same pinned dependency set.
      - name: Cache ingestion dependency wheels
        id: cache-ingestion-dependencies
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ingestion-pip-${{ runner.os }}-python-3.12-${{ hashFiles('.github/scripts/ingestion/requirements.txt') }}

      - name: Install ingestion dependencies
        id: install-ingestion-dependencies
        shell: bash
        run: |
          set -euo pipefail
          install_started=$SECONDS
          python -m pip install \
            --cache-dir "$HOME/.cache/pip" \
            --prefer-binary \
            -r .github/scripts/ingestion/requirements.txt
          install_seconds=$((SECONDS - install_started))
          echo "cache_hit=${{ steps.cache-ingestion-dependencies.outputs.cache-hit }}" >> "$GITHUB_OUTPUT"
          echo "duration_seconds=$install_seconds" >> "$GITHUB_OUTPUT"
          {
            echo "### Dependency installation measurement"
            echo
            echo "- pip cache hit: ${{ steps.cache-ingestion-dependencies.outputs.cache-hit }}"
            echo "- elapsed install time: ${install_seconds}s"
            echo "- cache key: OS + Python 3.12 + requirements hash"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Verify ingestion contract
        run: pytest -q .github/scripts/ingestion/test_ingestion.py

      # Cache keyed on the pinned revision, so the ~2-4 GB GGUF file only
      # downloads once per revision, not once per workflow run. This is
      # the alternative to committing the model file into the repository
      # (git hard-blocks files over 100 MB without LFS, and GitHub's free
      # LFS tier is 1 GB storage + 1 GB bandwidth/month — nowhere near
      # enough for a model file pulled on every ingestion run).
      - name: Cache model weights
        id: cache-model
        uses: actions/cache@v4
        with:
          path: models/${{ env.HF_MODEL_FILE }}
          key: model-${{ env.HF_MODEL_REPO }}-${{ env.HF_MODEL_REVISION }}-${{ env.HF_MODEL_FILE }}

      - name: Download model weights (cache miss only)
        if: steps.cache-model.outputs.cache-hit != 'true'
        run: |
          mkdir -p models
          python3 -c "
          from huggingface_hub import hf_hub_download
          import shutil
          path = hf_hub_download(
              repo_id='${{ env.HF_MODEL_REPO }}',
              revision='${{ env.HF_MODEL_REVISION }}',
              filename='${{ env.HF_MODEL_FILE }}',
          )
          shutil.copy(path, 'models/${{ env.HF_MODEL_FILE }}')
          "

      # This runs for both cold downloads and cache hits. It must remain
      # before conversion, segmentation, model initialization, and Issue
      # filing so a corrupt cache cannot create partial ingestion output.
      - name: Verify model weights
        run: |
          python3 .github/scripts/ingestion/verify_model.py \
            "models/${{ env.HF_MODEL_FILE }}" \
            "${{ env.HF_MODEL_SIZE_BYTES }}" \
            "${{ env.HF_MODEL_SHA256 }}"

      - name: Convert manuscript to markdown (Tier 0)
        run: |
          python3 .github/scripts/ingestion/convert_manuscript.py \
            "${{ inputs.manuscript_path }}" \
            /tmp/converted.md

      - name: Segment into chapters and scenes (Tier 0)
        run: |
          python3 .github/scripts/ingestion/segment_scenes.py \
            /tmp/converted.md \
            /tmp/segments.json

      - name: Extract draft capsules (Tier 1 — Phi-4-mini, CPU, local to this runner)
        run: |
          python3 .github/scripts/ingestion/extract_capsules.py \
            /tmp/segments.json \
            models/${{ env.HF_MODEL_FILE }} \
            /tmp/capsules.json

      - name: File capsules as draft GitHub Issues
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python3 .github/scripts/ingestion/file_capsules_as_issues.py \
            /tmp/capsules.json \
            "${{ inputs.owner }}" \
            "${{ inputs.repo }}"
